import React from 'react';
import './Footer.sass';

const footer = props => (
        <footer>
                <a href="diaryofjide.com">diaryofjide.com</a>
        </footer>
);

export default footer;
